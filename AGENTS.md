# AGENTS.md — 이 저장소에서 작업하는 코딩 에이전트용 상시 지시서

이 파일이 코덱스와 리뷰어(Claude) 사이의 유일한 연락 채널이다.
사람이 지시를 옮겨 적지 않는다. **매 세션 시작 시 이 파일을 먼저 읽는다.**

- 구현 지시서(상품 정의·자산 지도·전체 작업 목록): `KMONG_AI_AUTOMATION_PRODUCT.md`
- 기술 문서(잡 스키마·스케줄 동작·장애 대응): `docs/botkit.md`
- 작업 브랜치: `claude/ai-bot-automation-monetization-vimz44` (다른 브랜치에 푸시 금지)

---

## 0. 세션 시작 절차

```bash
git checkout claude/ai-bot-automation-monetization-vimz44
git pull --ff-only origin claude/ai-bot-automation-monetization-vimz44   # 리뷰 수정이 들어와 있다
pip install -e ".[dev]"
ruff check . && pytest -q        # 둘 다 통과하는 상태에서 시작해야 한다
```

`git pull`을 건너뛰지 마라. 리뷰어가 네 코드를 직접 수정해 푸시하는 경우가 있고,
아래 **4장 리뷰 기록**이 그 내용과 이유를 설명한다.

그리고 **아래 3장 작업 큐의 맨 위 항목 하나만** 수행한다. 여러 항목을 묶지 마라.

---

## 1. 절대 제약 (위반 시 상품성이 깨진다)

1. **납품 1건에서 새로 쓰는 파일은 `config/jobs/<고객사>.yaml` 한 장.**
   새 기능은 반드시 YAML 옵션으로 노출한다. 고객마다 파이썬을 고쳐야 하면 설계 실패다.
2. **`src/botkit`은 독립적이다.** `autocpna`·SQLAlchemy·DB를 import하지 않는다.
   의존 패키지는 `anthropic`, `httpx`, `pydantic`, `pyyaml`, `click` + 표준 라이브러리로 제한.
3. **초안까지만.** 봇은 사람에게 결과를 보내는 것으로 끝난다. 고객 명의로 외부에
   자동 게시·발송하는 기능은 추가하지 않는다.
4. **조용한 실패 금지.** 잘린 응답, 열 이름 오타, 시크릿 누락은 명시적 에러로 터진다.
   `KMONG_AI_AUTOMATION_PRODUCT.md` 4장의 방어 코드 목록을 제거·우회하지 않는다.

### 1-1. 제약 4에 우선하는 단일 원칙 — 회계는 발송을 막지 않는다

이력·비용·검증 같은 **내부 기록의 문제로 고객 발송을 멈추거나 되돌리지 않는다.**
발송된 메시지는 회수할 수 없으므로, 기록을 지키려고 재시도를 유발하면 고객이
같은 보고를 두 번 받는다. 기록 실패는 **운영자에게만** 올린다(종료 코드 1 +
`OPS_TELEGRAM_CHAT_ID`). 단, **발송 전(preflight)** 단계의 기록 실패는 실행을
중단시킨다 — 기록 없이 유료 호출이 나가는 경로는 없어야 한다.

이 원칙을 P0-1 리뷰에서 두 번 어겼다(4장 참고). 판단이 애매하면 이 절로 돌아온다.

---

## 2. 완료 기준 (DoD)

- [ ] `ruff check .` 통과, `pytest -q` 전체 통과
- [ ] 신규 기능마다 테스트 추가. docstring에 **"왜 이 케이스가 필요한가"**를 적는다
- [ ] **기존 테스트를 통과시키려고 수정하지 않는다.** 계약이 바뀌어 불가피하면
      원래 검증 의도를 유지한 채 갱신하고, 커밋 메시지에 그 이유를 적는다
- [ ] 새 환경변수는 `.env.example`과 `.github/workflows/botkit.yml`의 `env:` 블록에
      **양쪽 모두** 추가 (한쪽만 하면 로컬은 되고 Actions에서만 실패한다)
- [ ] 새 YAML 옵션은 `docs/botkit.md` 옵션 표와 `config/jobs/_TEMPLATE.yaml`에 반영
- [ ] 커밋은 기능 1개 = 커밋 1개. 메시지는 '무엇'이 아니라 **'왜'**를 적는다
- [ ] 푸시 후 **5장 보고 양식**대로 이 파일을 갱신하고 함께 푸시한다
- [ ] PR은 만들지 않는다 (요청이 있을 때만)

실제 유료 API 호출과 고객 채널 발송은 **사람의 승인 전에는 하지 않는다.**
합성 데이터와 목(mock)으로 검증하고, 승인 필요 항목은 5장에 적어 올린다.

---

## 3. 작업 큐

맨 위 항목 하나만 수행한다. 완료하면 리뷰어가 검증 후 다음 항목으로 큐를 갱신한다.

### ▶ 지금 할 일: P0-2 — 고객사별 시크릿 격리

`KMONG_AI_AUTOMATION_PRODUCT.md` 3장 P0-2 참고. 요구사항 요약:

- 지금은 한 저장소의 여러 잡이 `TELEGRAM_BOT_TOKEN` 하나를 공유한다.
  고객 A의 토큰으로 고객 B에게 보낼 수 있는 구조다.
- 잡 YAML에 `client_id` 필수 필드를 추가한다.
- 시크릿 이름 규칙: `TELEGRAM_BOT_TOKEN__<CLIENT_ID>`를 먼저 찾고, 없으면 공용
  `TELEGRAM_BOT_TOKEN`으로 폴백한다(기존 잡 호환). `chat_id`와 웹훅 URL도 동일 규칙.
- `botkit validate`가 잡별로 **어떤 시크릿 이름을 실제로 해석했는지** 출력한다
  (값은 출력하지 않는다). 폴백이 쓰였으면 그 사실을 표시한다.
- 고객 A의 잡이 고객 B의 토큰을 **참조할 수 없음**을 테스트로 증명한다.
- `docs/botkit.md`에 "고객사 1곳 = 저장소 1개"를 권장 운영 형태로 명시하고,
  한 저장소 다중 고객 운영의 위험(시크릿 공유, 한 고객 장애의 전파)을 적는다.

주의: `client_id`를 필수로 만들면 기존 예시 잡 3개와 `_TEMPLATE.yaml`도 갱신해야 한다.
`history.directory`를 `client_id`별로 분리할지도 함께 판단해서 근거를 적어라.

### 다음 (아직 시작하지 말 것)

1. **P0-3** 발송 전 미리보기 승인 모드 (`approval: {mode: auto | operator_first}`)
2. **P1-1** 구글 시트 되쓰기 (Apps Script 웹앱 경로)
3. **P1-2** 네이버 검색 API 소스
4. **P1-3** 텔레그램 인바운드 명령 (`/run`, `/pause`, `/status`)
5. **P2** 온보딩 자동화, 프롬프트 A/B 기록, 통합 테스트 분리

---

## 4. 리뷰 기록 — 되돌리지 말 것

리뷰어가 직접 수정해 푸시한 내용이다. 같은 실수를 반복하지 않기 위한 기록이다.

### P0-1 (커밋 `873804a`에서 수정됨)

**좋았던 판단** — 유지할 것:
- 워크플로에서 `${{ inputs.job }}`을 `run:`에 직접 보간하지 않고 `env:`로 넘긴 것
  (셸 인젝션 차단). 앞으로도 사용자 입력을 셸에 보간하지 마라.
- `httpx` 예외 문자열을 그대로 노출하지 않는 처리. URL에 봇 토큰이 들어 있다.
- 부분 발송 증거(`message_ids`)를 실패 경로에서도 보존한 것.
- 기존 테스트를 반환 계약 변경에만 맞춰 수정하고 원래 검증을 유지한 것.

**되돌려진 것 3건** — 다시 넣지 말 것:

1. **`run_job`의 `finally`에서 `history.append` 예외가 그대로 전파되던 것.**
   발송 성공 결과가 버려져 `run_due`가 failed로 기록 → state 미기록 → 다음 tick에
   **같은 회차가 고객에게 재발송**됐다(재현 확인). 이제 발송 후 장부 실패는
   `RunResult.ledger_error`와 종료 코드 1로만 올린다. 1-1절 원칙.
2. **캐시 usage가 0이 아니면 예외로 발송을 막던 것.** 플랫폼이 암시적 캐싱을 켜는
   순간 전 고객 보고가 동시에 멈춘다. 게다가 `GenerationResult("", None, None)`로
   방금 측정한 토큰을 버려 P0-1의 목적과 모순이었다. 이제 토큰은 남기고
   `cache_tokens_seen: true` + 비용 null로 표시하며 발송은 계속한다.
3. **텔레그램 에러에서 `_error_detail()`(응답 `description`)을 삭제한 것.**
   봇 토큰은 URL에만 있고 응답 바디에는 없다. `chat not found` 같은 실제 사유가
   사라져 `docs/botkit.md`의 장애 대응표와도 어긋났다. 복구했다.

**교훈 한 줄**: 정확성·안전을 위한 제약을 추가할 때, 그 제약이 **고객 발송 경로를
막는지** 먼저 확인하라. 막는다면 그건 운영자 알림으로 내려야 한다.

---

## 5. 보고 양식 (작업 완료 시 이 절을 덮어쓰고 푸시)

```
### 최근 작업 보고 — <작업 번호> (<커밋 해시>)
- 한 일:
- 새로 노출한 YAML 옵션:
- 새 환경변수 (양쪽 등록 여부):
- 기존 테스트를 수정했다면 어느 것을 왜:
- 검증: ruff / pytest 결과, 신규 테스트 수
- 사람 승인이 필요한 것 (유료 API 호출, 실제 발송, 외부 계정 설정):
- 리뷰어가 봐야 할 판단 지점:
```

### 최근 작업 보고 — P0-2 (`7d7153f`)

- 한 일: `2aaab6f`까지 pull --ff-only 후 P0-2만 구현했다. 고객 전용 이름 우선/공용
  폴백을 Telegram 토큰·chat ID·웹훅 URL 및 env 헤더에 적용했다. 타 고객 접미사를
  명시하는 설정/헤더 우회는 로딩과 실행 경계에서 차단한다. 잡 복사본의 이름만 바꿔
  A→B→A 실행 중 원본 잡/공용 환경변수가 섞이지 않는다. validate는 실제 선택한
  이름과 전용/폴백/누락만 표시한다. 예시 3개의 disabled 상태를 유지했다.
- 새로 노출한 YAML 옵션: 필수 `client_id`. 대문자로 시작하는 영문 대문자·숫자·단일 `_`,
  최대 64자. 기존 잡에도 추가해야 한다. `sinks.*_env`와 `env:` 헤더는 기존 옵션 그대로다.
- 새 환경변수 (양쪽 등록 여부): ACME 예시용 `TELEGRAM_BOT_TOKEN__ACME`,
  `TELEGRAM_CHAT_ID__ACME`, `SLACK_WEBHOOK_URL__ACME`, `CLIENT_ADMIN_TOKEN__ACME`를
  `.env.example`과 `.github/workflows/botkit.yml` env 양쪽에 등록했다. 값은 넣지 않았다.
  다른 고객 ID를 추가할 때에도 두 곳의 이름 매핑과 실제 Secrets 등록이 필요하다.
- 기존 테스트를 수정했다면 어느 것을 왜: `test_botkit_{jobspec,llm,runner,history}.py`
  합성 잡 fixture 및 jobspec의 합성 YAML에 새 필수 `client_id`만 추가했다.
  기존 assertion은 수정/삭제하지 않았다. 계약 변경 이유는 기능 커밋 메시지에 명시했다.
- 검증: 로컬 Ruff 통과 / pytest **260 passed (신규 45)**. 원본 215개도 통과했다.
  [기능 커밋 CI (Python 3.11)](https://github.com/NAMSOO-KIM/auto-cpna/actions/runs/35675449477)는
  Ruff 및 테스트 **260개 통과**를 확인했다. 실제 외부 API 대신 합성 데이터와 MockTransport만 사용했다.
- 사람 승인이 필요한 것: 실제 유료 Claude 호출, test-telegram/고객 채널 발송,
  고객 시스템 연결, Secrets 등록 및 운영 YAML/배포 변경. 이번에는 수행하지 않았다.
  변경/검증/배포/이슈/롤백은 [P0-2 릴리스 기록](docs/releases/p0-2.md)에 정리했다.
- 리뷰어가 봐야 할 판단 지점:
  - P0-1 리뷰의 ledger_error/발송 유지, 캐시 usage 보존/비용 null,
    Telegram description 복원 코드는 변경하지 않았다.
  - `history.directory`는 자동 분리하지 않았다. 기존 장부/report/백업 경로가 끊기지
    않도록 유지하고, 다중 고객은 YAML의 기존 옵션으로 `.botkit/ACME/history`를 지정한다.
  - 공용 폴백은 요구된 호환성이다. 토큰/채팅 ID가 각각 폴백할 수 있으며
    `__` 없는 별칭은 공용으로 취급한다. namespace 참조 차단을 OS/계정 보안 격리로
    과장하지 않았다. 고객사 1곳 = 저장소 1개 권장과 공용 시크릿/장애 전파 위험을 문서화했다.
  - ANTHROPIC_API_KEY와 운영자 알림 설정은 공용으로 유지한다. 전용 고객 토큰만
    넣어도 운영자 알림용 공용 봇 토큰/OPS chat은 별도 준비해야 한다.
  - 작업 큐는 리뷰어가 갱신하므로 그대로 두었다. P0-3 이후 작업은 시작하지 않았다.
